# Route only uhf-gatewayd's local0 messages to the product log.
if ($programname == 'uhf-gatewayd' and $syslogfacility-text == 'local0') then {
    action(type="omfile" file="/var/log/uhf-gateway/gateway.log")
    stop
}
