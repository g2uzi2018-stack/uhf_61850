(function () {
    "use strict";
    function redirect() { window.location.replace("/login"); }
    function load(path, target, empty) { return fetch(path, {credentials: "same-origin"}).then(function (response) { if (response.status === 401) { redirect(); throw new Error("auth"); } if (!response.ok) { throw new Error("request failed"); } return response.json(); }).then(function (payload) { target.textContent = ""; var entries = Array.isArray(payload.entries) ? payload.entries : []; if (!entries.length) { var text = document.createElement("span"); text.className = "muted-cell"; text.textContent = empty; target.appendChild(text); return; } entries.slice(0, 20).forEach(function (item) { var row = document.createElement("div"); row.className = "storage-row"; var name = document.createElement("strong"); name.textContent = item.name || "--"; var detail = document.createElement("span"); detail.textContent = item.generation ? "generation " + item.generation + " · " + (item.payload_status || "") : "事件 #" + item.id + " · " + (item.frame_count || 0) + " 帧"; row.appendChild(name); row.appendChild(detail); target.appendChild(row); }); }); }
    function loadAll() { return Promise.all([load("/api/v1/frames", document.getElementById("frames-list"), "尚无周期帧"), load("/api/v1/events", document.getElementById("events-list"), "尚无事件包")]).catch(function (error) { if (error.message !== "auth") { document.getElementById("toast").textContent = "存储索引暂时不可用"; document.getElementById("toast").classList.add("visible"); } }); }
    document.querySelectorAll('[data-action="logout"]').forEach(function (button) { button.addEventListener("click", function () { fetch("/api/v1/session", {method: "GET", credentials: "same-origin"}).then(function (response) { return response.json(); }).then(function (session) { return fetch("/api/v1/session", {method: "DELETE", credentials: "same-origin", headers: {"X-CSRF-Token": session.csrf_token}}); }).then(redirect).catch(redirect); }); }); loadAll();
}());
