# v3.0 独立审核交接

## 基线与范围

- 开发分支：`v3.0`。
- 本次实际获取的 GitHub 远端基线：`7b9b27fa0d1c7408f55b20ec3b49a23d83dcc2ab`。
- 同步时 `origin/v3.0` 仍为 `4e23631`，`github/v3.0` 已包含五个较新的 v3 提交；本次以 GitHub 目标仓库的最新提交为基线。开发增量随后同时推送到两个 `v3.0` 镜像，未强制覆盖历史。
- 本增量未修改原始 DOCX/XLSX、`main`、`v2.0`、目标板、`/data`、frpc/4G 或生产配置。

## 已完成的代码与测试对应关系

| 需求范围 | 实现 | 测试/证据 | 状态 |
|---|---|---|---|
| 局放 3 通道完整分段读取 | `src/v3/protocol.*`, `src/v3/acquisition.*` | `v3_protocol`, `v3_acquisition`；每通道 31 段、3615 寄存器、完整通道临时缓冲 | 已由 `GatewayRuntime --v3` 接入正式守护进程 |
| 电流/温度下行、串口与换算配置 | `acquisition/serial_settings.hpp`，`PosixSerialPort`，`PortCollector::poll_current`/`poll_temperature`，`ConfigStore`，`GatewayRuntime::apply_runtime_configuration`，`web/settings.*` | `uhf_serial_port_smoke` 验证 profile/termios；`uhf_config_store_smoke` 验证 profile、编码/倍率组合和重启；`uhf_v3_gateway_e2e` 验证 profile 未配置时不打开不存在设备，以及 Web 热配置换算后的工程值 | profile 保存后重启生效，换算热生效；默认全部未配置，真实值仍待厂商输入 |
| 错误帧、重试、迟到隔离与采集时序 | `PortCollector::read_registers`/`quarantine`，`AcquisitionScheduler`，`ConfigStore`，设置页 | PTY CRC 错误后重试；协议单测覆盖 CRC、短帧、异常码、长度和来源；`v3_acquisition` 验证热切换；正式 E2E 观测局放同通道 ≥2.99 秒且分段间不误加 3 秒 | 地址/周期/超时/重试/迟到隔离已接入并热生效；实际超时值和“重试 3 次”语义仍待厂商确认 |
| 三路互不阻塞、线程安全快照 | `AcquisitionScheduler`, `SnapshotStore` | 三个独立 worker；`uhf_v3_gateway_e2e` 以三个 PTY 同时驱动正式守护进程；快照并发读取测试 | 已接入；长期故障压力仍待目标环境验证 |
| 独立源时间与过期质量 | `v3::SourceStatus`, `FreshnessLimits`, `SnapshotStore::update_freshness_limits`，`ConfigStore`，`web/settings.*`，各上行适配器 | `uhf_config_store_smoke` 验证范围/持久化；`uhf_v3_upstream_smoke` 用显式单调时间验证热切换和 Web/Modbus 表示；`uhf_v3_gateway_e2e` 经正式 Web API 热加载并从源状态读回实际期限；IEC smoke 校验 `questionable/oldData` | 已接入并热生效；10 分钟/5 秒默认、1 秒至 24 小时范围及上行表现仍是未确认策略 |
| 35 项运算、质量传播、50 次窗口 | `src/v3/measurements.*` | `v3_measurements` | 已实现于 v3 核心 |
| Modbus PDU 与点表导出 | `src/v3/register_map.*`, `src/web/http_server.cpp`, `web/settings.html` | `v3_register_map` 核对 77 条分组映射；`uhf_web_server_smoke` 核对未登录 401、登录后 CSV 内容/下载头；`uhf_v3_upstream_smoke` | PDU 已挂正式 TCP/RTU；设置页可下载 `/api/v1/point-table/export.csv`，表和服务共用地址常量 |
| 正式 Modbus/Web 接入 | `src/modbus/tcp_server.cpp`, `src/modbus/rtu_server.cpp`, `src/web/v3_snapshot_json.cpp`, `src/app/gateway_runtime.cpp` | `uhf_v3_upstream_smoke`；`uhf_v3_gateway_e2e` 经第四个 PTY 从正式 RTU 服务读取 Ia float32、Ia 报警并校验 CRC；`--v3` 运行入口 | TCP/RTU/Web 均已挂接 |
| 三合一实时页面与推送 | `web/overview.{html,js}`、`web/styles.css`、`HttpServer::run_websocket` | `uhf_web_preview_smoke` 排除 PD1000/dBm 并核对三通道/35 项/报警元素；`uhf_web_server_smoke` 核对正式静态资源；`uhf_v3_gateway_e2e` 从三个 PTY 驱动的正式守护进程 WebSocket 读取 schema 3、三通道和 35 项值 | 页面消费正式后端数据，三通道可切换；谱点只显示原始值且明确物理单位待确认；未用前端假数据 |
| 遥信/报警 | `src/v3/acquisition.*`, `src/config/config_store.*`, `web/settings.*`, `src/modbus/*`, `src/iec61850/server.cpp` | `uhf_config_store_smoke` 验证 12 项有限数值/空值、保存失败保持旧配置和重启复核；`uhf_v3_gateway_e2e` 通过正式 Web API 保存并热加载电流/温度阈值；`v3_acquisition`、`uhf_v3_upstream_smoke`、`uhf_iec61850_v3_model_smoke` | 12 点严格 `>` 阈值和三路通信状态已接入；无产品默认值的点保持未配置/质量无效 |
| IEC 61850 v3 | `src/iec61850/{scl_model,model,server}.*` | `uhf_iec61850_v3_model_smoke` 读取值/q/t、41 项测量与 15 项状态数据集，验证 URCB/BRCB、通信告警和过期转换；`uhf_v3_gateway_e2e` 从正式三 PTY 守护进程把 Ia 10→30 送到实际 MMS 读值和 data-change 报告 | 候选服务模型、两个数据集、两类报告和正式主程序链路已接入；最终点表和目标上位机报告互操作未验收 |
| v3 历史存储 | `src/storage/v3_history_store.*`, `src/storage/persistence_runtime.*`, `src/web/http_server.cpp`, `web/storage.*` | 格式/CRC/失败恢复/清理单测；正式 E2E 验证实际 `data/v3` 文件、schema 3 索引、35 行 CSV、未配置 Ia 的无效质量、旧事件接口显式 unsupported/404 | 原子记录、三源 UTC、质量、查询、CSV 和故障生命周期已接入；存储页按 schema 展示 v3 字段，不继承旧 dBm 事件目录；未做目标盘真实耗尽和长期压力验证 |
| Web 运行模式隔离 | `web/settings.*`, `web/storage.*`, `HttpServer::events_json`/`latest_event_csv` | `uhf_web_preview_smoke` 核对默认隐藏及 schema 切换标记；兼容 Web smoke 验证无数据目录仍保持旧 503；`uhf_v3_gateway_e2e` 验证后端隔离 | v3 页面不展示 PD1000 采集、dBm 门限、旧事件包或 3615 寄存器说明；兼容入口仍保留原功能 |
| 内存报文查看 | `src/v3/packet_trace.*`, `src/app/gateway_runtime.cpp`, `src/web/http_server.cpp`, `web/logs.*` | `v3_packet_trace`/`uhf_v3_packet_trace_smoke`；`uhf_v3_gateway_e2e` 校验三源 TX/RX、容量和 data 目录无报文文件；Web smoke 校验未登录 401 和非 v3 503 | 256 条/64 KiB 双硬上限，丢旧计数，仅内存；Web 复制后序列化，不阻塞生产者 |
| 正式主程序端到端 | `src/main.cpp`, `tests/v3_gateway_e2e_smoke.py` | `uhf_v3_gateway_e2e` | 三个下行 PTY + 一个上行 RTU PTY → 真实守护进程 → 原子 Web 配置/热加载采集时序、换算、报警和三路时效 → HTTP/WebSocket schema 3、Modbus/TCP 与 RTU 的 Ia 实值、IEC MMS 读值与报告；同时观测局放完整通道轮次间隔和分段连续读取；未替代实机验收 |
| 可交互主机模拟入口 | `tools/v3_host_sim.py` | `uhf_v3_host_sim_smoke` 运行真实守护进程并从 HTTP 快照核对三路样本、schema 3 和工程换算 | 每次生成随机临时测试凭据，只监听 localhost，退出后清理；不安装到目标板且不形成生产默认绕过 |
| Release 测试完整性 | `tests/test_check.hpp` 与 9 个 host C++ smoke | Release 全目标构建；Release 全量 72/72；受影响 9 项在 Debug/Release/ASan+UBSan 并行复跑均 9/9 | 检查不再被 `NDEBUG` 删除；临时目录按 PID 隔离；只改测试设施 |
| 激活门禁 | `src/activation/{activation_manager,credential_file}.*`, `src/app/gateway_runtime.cpp`, `src/main.cpp` | `uhf_activation_smoke` 覆盖绑定生命周期和文件安全；`uhf_v3_gateway_e2e` 通过正式入口覆盖成功、错误码、重启绑定不匹配，并断言失败时三路 PTY 无请求且 Web、TCP、RTU、IEC 均不可用 | `--v3` 在串口/服务创建前要求有效绑定；文件式输入避免值进入进程参数；systemd 已使用文件参数，真实制造密钥和身份来源未验证 |
| v3 安装与正式启动 | `packaging/systemd/uhf-gateway.service`、`packaging/install.sh`、`config/v3-runtime.env.example` | systemd/install-tree/release-build/installer/packaging-policy smoke 5/5；覆盖模板占位拒绝、外部制造文件、升级保持、权限和动态设备 allowlist | 随包单元只启动 `--v3`；首次正式安装缺参数会在切换前失败，不回退旧入口；仍未在目标板执行安装 |

## 可复现命令

```sh
cmake -S src/v3 -B build/v3 -DCMAKE_BUILD_TYPE=Debug
cmake --build build/v3 --parallel 4
ctest --test-dir build/v3 --output-on-failure
```

本次实际结果：5/5 v3 测试通过。根工程 IEC 61850 host 回归也实际运行：

```sh
cmake -S . -B build/host -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Debug -DUHF_ENABLE_IEC61850=ON
cmake --build build/host --parallel 4
ctest --test-dir build/host --output-on-failure
```

最新增量实际执行 Debug、Release、ASan+UBSan 三套独立 v3 构建，各 5/5 通过；根工程（显式 `-DUHF_ENABLE_IEC61850=ON`）现在有 72 项，最终 Debug 与 Release 均 72/72，其中包含三个 PTY 的真实守护进程端到端测试、MMS v3 时间/质量读取、点表导出、内存报文缓存、激活和历史持久化。`-DUHF_ENABLE_IEC61850=OFF` 的一次基线构建不能编译 `uhf-gatewayd`，原因是现有 CMake 在关闭 IEC 选项时仍引用 IEC 头文件；该命令不是本次代码路径，之后使用 IEC 配置完成了完整回归。

v3 参数配置增量还在根工程 Release 构建执行上行、配置、Web、正式守护进程 PTY 端到端四项（4/4），在 ASan+UBSan 根工程构建执行上行、配置和正式守护进程 PTY 端到端三项（3/3）。Release 首次运行上行测试发现旧断言依赖自然经过 1ms，优化构建可能尚未到期；测试改用显式旧单调时间后 Debug/Release/ASan+UBSan 均重跑通过。较早的 Release 串口 smoke `assert`/fixture 问题也已修正；这些如实记录为测试设施问题，不是产品路径失败。

采集时序增量首次 Release E2E 在等待第二轮局放后，256 条内存缓存正常淘汰了早期电流/温度条目，旧的三路同时存在断言因此失败。测试顺序已改为先验证三路缓存上限/内容，再使用 PTY 独立验证局放轮次间隔；最终 Debug 全量 71/71、Release 4/4、ASan+UBSan 3/3。未扩大缓存或削弱容量断言。

三合一 Web 增量最终执行：`ctest --test-dir build/host --output-on-failure` 当时为 71/71；Release 先构建 `uhf-gatewayd`，再运行 `uhf_web_preview_smoke|uhf_web_server_smoke|uhf_websocket_smoke|uhf_v3_gateway_e2e` 为 4/4；ASan+UBSan 构建 `uhf-gatewayd` 与 Web smoke 所需的 `uhf-privilegedd` 后同组 4/4。Release 当时的一次全目标构建因 `NDEBUG` 移除测试 `assert` 后触发 `-Werror`，正式网关目标不受影响；该测试设施问题已由后续显式检查增量修复。ASan 首轮定向测试曾因尚未构建 `uhf-privilegedd` 而 3/4，补建依赖后失败项重跑通过，最终同组为 4/4。

文件式激活输入增量在 Debug、Release、ASan+UBSan 中都构建 `uhf-gatewayd uhf_activation_smoke`，并运行 `uhf_activation_smoke|uhf_v3_gateway_e2e`，三套均 2/2；E2E 的测试值只写入临时目录的 `0600` 文件，命令行不再携带身份、密钥或激活码。

v3 安装启动增量在 host Debug、Release、ASan+UBSan 分别运行 systemd policy、install tree、release build、installer、packaging policy 五项，三套最终均 5/5。Release 首轮仅构建网关，install-tree 因尚无 `uhf-network-recovery` 安装产物而 4/5；补建五个正式安装目标后失败项重跑通过。安装器测试只在临时根目录组装、首次安装和升级，没有调用真实 systemd 或写主机 `/etc`；目标部署明确未执行。

交互式主机模拟增量通过 `python3 tools/v3_host_sim.py` 启动；自动测试为 `uhf_v3_host_sim_smoke`，会验证正式 HTTP 快照后等待模拟器就绪，再发送 SIGTERM，并要求守护进程、PTY 和临时凭据正常清理。手工首跑发现原固定默认端口与本机容器冲突，已改成默认自动选择空闲 localhost 端口；ASan 首次终止复跑还暴露就绪文字的测试竞态，现以管道标志同步。最终该测试在 Debug、Release、ASan+UBSan 均为 1/1 通过，Debug 根工程全量为 72/72；这些仍只证明主机模拟路径。

v3/兼容页面隔离增量在 Debug、Release、ASan+UBSan 都运行 `uhf_web_preview_smoke|uhf_web_server_smoke|uhf_v3_gateway_e2e`，最终三套均 3/3。Debug 首轮的新增兼容断言曾把未配置数据目录时的既有 503 错写成 200，修正为明确验证 503；v3 正式 E2E 则要求事件索引为 schema 3 `supported:false` 且旧事件导出为 404。

Release 测试完整性增量将 9 个仍使用 C `assert` 的 C++ smoke 切换到始终启用的 `UHF_TEST_CHECK`。最终 `cmake --build build/host-release -j2` 全目标成功，`ctest --test-dir build/host-release --output-on-failure` 为 72/72。首次并行跑 Debug/ASan 受影响 9 项时固定 DHCP 临时目录冲突，Debug 8/9、ASan 9/9；将相关网络临时目录按 PID 隔离后，Debug、Release、ASan+UBSan 并行复跑均 9/9。

IEC 报告验证增量在 `build/host`、`build/host-release`、`build/host-sanitize` 中分别执行 `cmake --build BUILD -j2 --target uhf_iec61850_server_smoke` 和 `ctest --test-dir BUILD --output-on-failure -R '^uhf_iec61850_v3_model_smoke$'`。测量 URCB 阶段三套均 1/1，Debug、Release 全量均为 72/72；增加状态 BRCB 后三套再次均为 1/1，Debug 的兼容/v3 服务端回归为 2/2。它使用实际 MMS 网络客户端读取 41 项测量和 15 项状态数据集、启用候选未缓冲测量报告与缓冲状态报告，既验证 Ia=9.0 的 `data-change` 成员，也验证三次电流失败产生通信告警报告及成功样本清除服务值；不是只检查 ICD 文本，也不等于目标上位机互操作验收。

正式主程序 IEC 增量在三套根工程构建中构建 `uhf-gatewayd uhf_iec61850_server_smoke`，再执行 `ctest --test-dir BUILD --output-on-failure -R '^uhf_v3_gateway_e2e$'`，Debug、Release、ASan+UBSan 均 1/1，最终源码 Debug 全量为 72/72。测试主实例启用独立 localhost MMS 端口；客户端确认 41 点数据集并启用 URCB 后，PTY 将 Ia 从 10 改为 30，随后要求报告成员为 30、原因是 `data-change`，并从同一守护进程读回 30/`good`。因此该证据经过 `src/main.cpp` 和 `GatewayRuntime --v3`，不是只直接构造 IEC 类；边界仍是主机模拟和仓库候选客户端。

正式主程序 RTU 增量给同一 E2E 增加第四个 PTY并作为 `--modbus-rtu-device` 启动，不再关闭上行 RTU。Ia 变为 30 后，功能码 0x03/地址 1/长度 2 的响应被独立解码为 float32 30.0；功能码 0x02/地址 6/长度 1 返回 Ia 高报警 1，两个响应 CRC 均核对。Debug、Release、ASan+UBSan 的 `uhf_v3_gateway_e2e` 均为 1/1；非法请求、错误 CRC 和错误单元地址由既有 RTU/点表测试承担。

正式主程序 TCP 增量对同一 Ia 地址发出独立事务号的 0x03 请求，核对完整 MBAP 和 PDU 元数据，并将两个响应寄存器按高字在前 float32 解码为同轮 IEC/RTU 的 30.0（无 IEC 构建路径为 10.0）。因此正式 TCP 证据不再只是功能码和字节数；非法 MBAP、功能码和地址边界仍由既有 TCP/点表测试承担。正式 E2E 在 Debug、Release、ASan+UBSan 各为 1/1，无 IEC helper 的兼容执行也通过。

正式历史增量要求主进程产生 `data/v3/*.bin`，鉴权 API 返回 schema 3 非空索引，CSV 含表头加 35 项数据，且初始未配置倍率的 Ia 行为 `valid=0`/空值。新增下载头断言首轮失败，实际值为缺少闭合引号的 `attachment; filename="latest-frame.csv`；修复 `HttpServer` 的共享导出头后，`uhf_web_server_smoke|uhf_web_live_smoke|uhf_v3_gateway_e2e` 在 Debug、Release、ASan+UBSan 均为 3/3。该测试证明首次周期记录的正式落盘/查询/导出，不代表目标盘耗尽或长期保留验收。

`f902808` 的 Actions 三个 v3 core job 成功；host 72 项为 71 成功、1 失败，失败项是旧 `uhf_runtime_modbus_rtu_smoke` 的 2 秒 RTU 响应超时，而新增正式 v3 E2E 成功。旧测试未在启动网关前把 PTY 切为 raw，CI 并行负载放大了请求早于网关 termios 配置的竞态；修复为测试先对 PTY 两端 `tty.setraw`，保持原超时。随后本地按 CI 并行度重复旧 RTU 与 v3 E2E 各 10 次全部通过，Release 和 ASan+UBSan 各 2/2，最终源码 Debug 并行全量为 72/72；后续 Actions 状态需按新提交核对。

修复提交 `5b351f5` 的 Actions 运行 `35443323814` 已全部成功：三个 v3 core Debug/Release/Sanitizers 作业和包含旧 RTU smoke 的 72 项 host 回归均为绿。

`1415a26` 的 Actions 运行 `35443564204` 也全部成功。`889b824` 的运行 `35443919703` 三个 v3 core 作业成功，host Test 则达到 20 分钟上限而取消；上传 checkpoint 证明正式 v3 E2E 已通过，唯一未结束的是既有 DHCP smoke，另有既有持久化 smoke 的 5 帧断言失败。前者用 `O_CLOEXEC` 管道替代固定 50ms 子进程 exec 猜测，并将回收改为 2 秒有界；后者在每次发布后等待保存计数推进，仍要求事件包严格为 5 帧且总保存数为 5。Debug 两项各重复 10 次通过，Release、ASan+UBSan 各 2/2，最终 Debug 并行全量 72/72；没有删除或放宽原断言。

正式激活门禁测试还以错误激活码启动一次，并在成功激活状态上用不同设备标识重启一次；两次正式进程都必须返回激活错误。测试要求失败前后三个下行 PTY 请求计数不变，错误首激活不产生状态文件，确认 Web、Modbus/TCP 和启用时的 IEC 端口未监听，并要求独立上行 PTY 对合法 RTU 请求无响应。这是后端主入口证据，不依赖网页按钮可见性；正式 E2E 在 Debug、Release、ASan+UBSan 各为 1/1，无 IEC helper 路径也通过。真实制造参数仍未提供。

## 正式程序入口与当前边界

当前正式入口仍是 `src/main.cpp` 启动的 `uhf-gatewayd`。使用 `--v3 --v3-pd-device PATH --v3-current-device PATH --v3-temperature-device PATH` 时，它先复核设备绑定激活，再创建三个真实串口、`v3::AcquisitionScheduler`、v3 Modbus/TCP/RTU 分支、候选 IEC 三合一模型、v3 Web 快照和 `data/v3` 历史存储；不传 `--v3` 仍保持旧 PD1000 兼容路径。激活无效时在串口及所有 v3 服务创建前失败，不是只隐藏网页按钮。

审核者可在构建 `build/host/uhf-gatewayd` 后执行 `python3 tools/v3_host_sim.py`，使用终端打印的一次性账号访问 localhost 页面；此脚本只是把三路 PTY 接到同一正式入口，不是另一个演示后端。

实时总览已替换旧 PD1000/单通道/dBm 展示逻辑，形成由正式 schema 3 和 WebSocket 驱动的三通道局放、电流、温度、35 项计算值、质量和报警页面。下一阶段仍需完成目标盘压力、最终 IEC 点表/报告互操作和制造激活参数。生产所需电流/温度 profile、编码倍率、谱点单位、报警默认阈值、无效/过期最终约定和 IEC 点表仍需产品输入。

设置页和存储页通过正式 `/api/v1/snapshot/latest` 的 schema 识别运行模式。v3 只展示三路采集、换算、时效、12 点阈值和三合一历史字段；兼容配置仍保留在同一原子配置文件中但不会作为 v3 产品参数展示。v3 后端不会读取或导出旧 `data/events`，因为新需求没有确认 dBm 门限和事件包语义；是否需要新的 v3 事件策略仍是具体产品缺项。

## 已知暂定策略

- 新状态目录的总览标题默认改为“综合监测管理机 / 三合一监测设备”；已有配置文件中的用户自定义标题保持原值，不做覆盖式迁移。
- `CollectorOptions::max_retries` 表示首次尝试之外的重试次数，默认 3 且可热配置为 0–3；需求“重试 3 次”尚未明确总尝试次数。
- 局放 3 秒已按原文解释为同一完整通道两次读取的开始间隔，而不是同一通道 31 个分段请求的逐笔延时。`v3_pd_interval_ms` 下限固定为 3000；正式 PTY E2E 验证相邻轮次间隔及分段连续性，仍需实机确认设备准备时点。
- 电流/温度换算配置为 `v3_current_encoding` 和成对 multiplier/offset；编码只允许 `unconfigured`、`unsigned16`、`signed16`，倍率必须为正有限数。默认未配置时只记录下行通信，不发布看似正常的工程值。
- `uhf-gatewayd --v3` 的成对倍率命令行参数保留为主机模拟显式覆盖；缺一项或非正倍率在启动前拒绝。生产路径优先使用版本化原子配置，配置热切换会清空受影响值，电流切换还会重置 50 次窗口。
- 局放和上行 RTU 依据原文使用 115200/8N1；电流/温度 profile 默认 `unconfigured`，不会再默认为 115200。支持的 profile 为常见 termios 波特率加 `7/8 + N/E/O + 1/2`，例如 `9600/8E1`；配置变更明确返回需重启，主机 PTY 可用对应命令行参数覆盖。
- 12 个报警阈值通过 `v3_alarm_thresholds` 定长数组持久化；数值严格按 `>` 比较，`null` 在运行内表示 NaN 且报警有效位为 0。仓库默认全部为 `null`，测试阈值不是生产默认值。
- 激活测试使用仓库外临时测试密钥；仓库不包含默认密钥。生产应通过 `--v3-device-id-file`、`--v3-activation-key-file` 和首次激活时的 `--v3-activation-code-file` 提供外部文件，避免值出现在进程参数；内联选项仅为兼容和主机开发保留，同一个值同时指定内联与文件会在启动前拒绝。受保护文件必须为 root 或当前服务用户所有的普通非链接文件，不能组可写或对其他用户可读写，且只能包含一个不超过 256 字节的非空值。缺必要值时后端拒绝 v3 启动。
- 三源时效默认局放 10 分钟、电流 5 秒、温度 5 秒，通过 `v3_pd/current/temperature_freshness_ms` 原子保存并热生效，Web 回显实际期限；1 秒至 24 小时范围是可逆安全边界。判定基于单调时钟，不受系统校时倒退影响。Web 将过期值置 `null`，Modbus 返回异常 04，IEC 保留最后有限值并标 `questionable/oldData`；默认值、范围和上行约定均未标记为用户确认。
- 历史格式当前写 v2（增加三源 UTC 和 stale 位），读取器兼容本轮较早提交产生的 v1；v1 没有源 UTC，读取为未设置而不伪造记录时间。
- 报文查看缓存固定为最多 256 条且载荷总计不超过 64 KiB，只记录三路下行串口实际成功收发的字节块；`read_some` 可能把一个 Modbus 应答分成多个条目。修改上限的入口为 `PacketTraceBuffer` 构造参数；没有报文落盘或导出接口。
- 快照失败时将对应值标为不可用；局放失败通道的原始字不会作为新鲜值发布。上述暂定上行选择仍待最终确认。

## 未验证项

没有目标板、实际三类下位机、上位机 Modbus/IEC 客户端互操作、网络切换、目标盘真实耗尽、激活码制造配置或 72 小时稳定性验证。上述项目不能由当前 host 测试替代。
