# v3.0 实现状态与需求审查

## 0. 结论与边界

当前分支是 `v3.0`。主需求足以开始分模块开发，但不足以在没有协议补充和实机联调的情况下认定整机交付完成。

本轮已经实现并测试**三端口采集、可热配置的采集时序/错误隔离/源时效、统一快照、Modbus/Web API 与正式三合一实时页面、显式串口 profile、工程换算与 12 点报警、候选 IEC 61850 v3 服务模型、v3 历史存储/查询、内存报文查看、后端激活总门禁与未激活专用页面、可交互三 PTY 主机模拟器及显式制造配置后才启动的 v3 systemd/安装链路**。仍不是整机 v3.0：最终 IEC 点表/互操作验收、厂商参数和目标硬件验证尚未完成。没有变更真实目标板、`/data` 服务、frpc、4G 或生产配置。

## 1. 唯一开发基准

1. `spec/v3.0/嵌入式开发需求书-综合监测管理机-v3.0.docx`：当前主需求，含第八章对历史笔误的修正。
2. `spec/v3.0/电缆局放通信规约.docx`：新局放设备的原始规约，用于补充寄存器和设备参数定义。
3. `spec/reference/开发需求20240829-1.docx`：只用于核对电流/温度历史报文，不自动覆盖新需求。
4. 旧 v1.0、PD1000 Excel、`luna-implementation-brief.md`、`requirements-baseline.md`、旧架构/计划等，只作历史实现参考；与本次 v3.0 不一致的内容不得继续当作产品需求。

本轮没有改写任何原始 DOCX/XLSX，下面的临时实现选择不代表需求方已经确认。

## 2. 已完成的可测试增量

| 模块 | 已实现 | 测试 |
|---|---|---|
| `src/v3/protocol.*` | 三通道分别从 10001/15001/20001 开始、每通道 3615 寄存器的请求计划；电流/温度请求；应答长度、CRC、来源、功能码、异常码校验；局放七特征、3600 个有符号谱点及接收有效位；温度取 1/3/5 号寄存器 | 93 条局放请求覆盖、历史电流/温度请求金样本、错误帧、类型/范围/倍率边界 |
| `src/v3/measurements.*` | 35 项数据计算、三相各自有界 50 样本窗口、重复/乱序样本拒绝、有效性传播、float32 高字节/高字在前编码 | 35 项正常值、49/50/51 次边界、无效样本、除零、负温度均值、端口数据隔离 |
| `src/v3/register_map.*` | 无网络依赖的 0x03/0x04/0x02 读请求处理；70 寄存器和三通道镜像任意合法分段读取；15 点遥信编码；由同一地址常量生成包含 35 项 float、三通道特征/保留区/谱图范围和 15 点遥信的完整 CSV 点表 | 半个浮点值起读、边界/异常码、1500 次随机合法镜像读取、10000 次畸形请求、无效数据和 77 条分组点表映射 |
| `src/v3/acquisition.*`、`acquisition::ReconnectingSerialPort` | 每个物理串口独立的局放/电流/温度 worker；v3 下行 tty 启动缺失或 I/O 断开时只使对应源失败，后续采集尝试重新打开并恢复；局放按完整通道临时缓冲后原子发布，并按每个通道上次开始时间保证相邻完整读取 ≥ 配置间隔，31 个分段请求之间不误加 3 秒；CRC、短帧、异常帧、超时和写失败重试；坏帧后有界隔离迟到字节；线程安全统一快照和通信告警；三源独立时间/时效；运行中可安全更新周期、超时、重试和隔离参数 | `uhf_serial_port_smoke` 用可替换 tty 符号链接验证启动缺失、PTY 断开和新 PTY 恢复；`v3_acquisition` 验证 active settings、故障恢复与热切换；`uhf_v3_gateway_e2e` 先验证电流稳定路径启动缺失、运行中出现和未知换算保持无效，再让正式在线电流 PTY 断开至三次轮询失败，要求局放/温度仍在线、通信告警和健康 `down`，替换 PTY 后清零失败、健康 `up`、Ia=42；并观测局放同通道两轮间隔 ≥2.99 秒且相邻分段 <1 秒；故障测试覆盖 CRC/短帧/异常/超时 |
| `GatewayRuntime --v3`、`ConfigStore`、`src/modbus/*`、`src/web/*`、`web/overview.*`、`web/settings.*` | 局放固定 115200/8N1，电流/温度串口 profile 未知时不打开设备；局放地址、局放完整通道间隔、电流/测温周期、响应超时、重试延时、迟到帧隔离期、额外重试次数、工程换算、三路时效和报警阈值均原子保存并热加载；Web 源状态回显实际运行参数；实时页按 schema 3 展示三通道局放、35 项值/质量、三源独立时间和 12 点报警；设置页按正式快照 schema 隔离 v3 与旧采集/事件选项；Modbus/TCP/RTU 复用 v3 点表 | 串口/config/Web smoke；`uhf_v3_gateway_e2e` 验证未配置设备隔离、正式 WebSocket schema 3 和热加载，从正式 TCP/RTU 服务读取 Ia float32，并经第四个 PTY 读取 Ia 报警和核对 CRC；同一正式 TCP 还验证电流断线时通信故障遥信置位、替换设备后复归且 Ia 更新为 42；设置页 schema/重启标志/运行模式标记测试 | 主程序数据 API、WebSocket、Modbus TCP/RTU 和三合一页面已接入；v3 操作界面不再展示旧 PD1000/dBm 设置，谱点只标设备原始值；真实电流/温度 profile、编码/倍率和单位仍须厂商提供 |
| `ReconnectingSerialPort`、`ModbusRtuServer`、`GatewayRuntime` 上行 RTU | 上行 tty 启动缺失不再阻止采集、Web、Modbus/TCP 或 IEC；设备路径出现或致命 I/O 后按原配置重开，缺失时服务循环采用 100 ms 有界退避避免忙等；断线时丢弃旧端口残帧；健康接口依据线程安全的实际连接态在 `down/up` 间切换 | `uhf_v3_gateway_e2e` 启动时故意不创建 RTU 稳定路径，在正式采集/Web/IEC 已运行后先核对健康为 `down`，再创建指向第四个 PTY 的链接，等待进程实际打开后核对健康为 `up`、0x03 浮点值、0x02 报警和两帧 CRC；随后向旧 PTY 只发送半帧并断开，核对健康回到 `down`，替换 PTY 后再次为 `up` 且完整请求不受旧半帧污染；`uhf_runtime_modbus_rtu_smoke` 在 IEC ON/OFF 均运行 | 已完成主机 PTY 启动隔离、断线、设备替换、恢复和状态生命周期；目标板 RS485 收发方向控制、设备节点重建及上位机现场轮询未验证 |
| `SnapshotStore`、`ConfigStore`、`web/settings.*`、`src/iec61850/{scl_model,model,server}.*` | 严格 `>` 阈值的 12 点测量报警；每点阈值可为有限浮点数或 `null`，未配置时质量无效；三路时效基于各自单调成功时间并可在 1 秒至 24 小时范围热加载，Web 状态回显实际期限；v3 SPDC1..3、MMXU1 35 项、STMP1 3 项、GGIO1 15 点含 q/t 的服务端更新；IEC 过期自动转 `questionable/oldData` 并保留最后值；候选测量/状态数据集及 URCB/BRCB 可由真实 MMS 客户端读取、启用并接收数据变化报告 | `uhf_config_store_smoke` 覆盖时效和阈值校验/持久化；`uhf_v3_upstream_smoke` 验证时效；`uhf_iec61850_v3_model_smoke` 核对 41 项测量与 15 项状态数据集、两类报告和通信告警恢复；`uhf_v3_gateway_e2e` 还从正式三 PTY 守护进程把 Ia 10→30 送到 MMS 读值和 data-change 报告 | 配置、候选报告及正式主程序链路已接入；默认期限、阈值和最终 IEC 点表仍待产品输入，目标上位机互操作未验收 |
| `src/storage/v3_history_store.*`, `PersistenceWorker`, `/api/v1/frames`、`/api/v1/events`、`web/storage.*` | v3 快照按 CRC 校验的原子二进制记录保存到 `data/v3`，保留测量值、PD 特征/谱点、质量、报警位和三源 UTC 时间；格式 v2 仍可读取已发布的 v1 文件；写入失败置运行故障并进入健康状态，后续成功写入自动恢复；查询和最新 CSV 导出复用 Web 存储接口；清理器按保留期/低空间处理 `v3-*.bin`；v3 页面展示真实字段，旧 dBm 事件索引明确返回 `supported:false` 且不读取遗留目录 | `uhf_v3_history_store_smoke` 覆盖格式与失败；`uhf_persistence_runtime_smoke` 覆盖写故障/恢复；`uhf_storage_cleaner_smoke` 覆盖保留期和低空间；正式 `uhf_v3_gateway_e2e` 验证生成 `data/v3/*.bin`、schema 3 索引、35 行 CSV 及未配置 Ia 保持无效，Web smoke 验证兼容路径 | 已接入 v3 周期历史链路且未继承旧事件门限；目标盘压力、真实磁盘耗尽、长期运行及是否新增 v3 事件策略仍未确认 |
| `src/v3/packet_trace.*`, `V3SerialAdapter`, `/api/v1/packets`, `web/logs.*` | 三个真实下行适配器记录实际 TX/RX 字节；线程安全环形缓存同时限制 256 条和 64 KiB，超限丢最旧并计数；Web 先复制有界快照再生成十六进制 JSON，慢客户端不持有采集锁；仅驻内存，无落盘代码路径 | `v3_packet_trace`/`uhf_v3_packet_trace_smoke` 覆盖条数/字节上限、超大输入、并发生产者和读取快照隔离；`uhf_v3_gateway_e2e` 通过正式守护进程验证三源双向报文和无报文文件；Web smoke 覆盖鉴权边界 | 已接入主程序和日志页面；展示的是底层串口 `read_some` 收发块，可能是一帧的分片 |
| `tools/v3_host_sim.py` | 每次创建三路 PTY、随机测试身份/密钥/激活码和临时状态目录，启动真实 `uhf-gatewayd --v3`；只监听 localhost，退出时清理子进程和临时数据 | `uhf_v3_host_sim_smoke` 从正式 `/api/v1/snapshot/latest` 验证 schema 3、三路样本和显式工程换算 | 主机交互验证入口；不安装到目标板，不提供生产激活绕过 |
| `tests/test_check.hpp` 及 9 个 host C++ smoke | 用始终启用的显式检查替代会被 Release `NDEBUG` 删除的 `assert`；网络类测试临时目录加入 PID，允许不同构建并行执行 | Release 全目标构建；Release 全量 72/72；Debug/Release/ASan+UBSan 受影响 9 项并行复跑均 9/9 | 测试设施修复，不改变产品运行代码；Release 现在实际执行表达式而不是空跑 |
| `src/activation/{activation_manager,credential_file}.*`, `src/main.cpp`, `src/web/http_server.*`, `web/activation.*`, `GatewayRuntime --v3` | 设备标识 + 外部密钥计算 HMAC-SHA256 截断 Base32 激活码；OpenSSL 常量时间比较；状态文件原子持久化；未绑定时只开放设备标识、激活提交和三项静态资源，禁止其余 API/WebSocket；成功落盘并再次复核后同一进程才创建正式运行时；设备标识和厂家密钥从防链接、受权限检查的单行文件读取 | `uhf_activation_smoke` 覆盖算法、末字节错误、错误长度、绑定生命周期和文件安全；`uhf_v3_gateway_e2e` 从正式入口核对激活页/标识、同源限制、错误码不落盘、激活前 PTY/TCP/RTU/IEC 隔离、正确码 `0600` 持久化、随后完整链路启动、绑定不匹配和旧 Web-only 绕过 | 原文 §9.6 的专用页面和后端总门禁已接入；每地址 60 秒窗口内第 5 次失败后阻断 30 秒是可逆暂定安全策略；真实制造密钥、设备标识来源和最终激活格式仍待确认 |
| `packaging/systemd/uhf-gateway.service`、`packaging/install.sh`、`config/v3-runtime.env.example` | 正式单元固定启动 `--v3`，只从文件读取运行 tty、设备标识和厂家密钥，不再要求或传递首次激活码；首次系统安装缺任一外部制造文件即在切换 release 前失败；模板占位、额外环境变量和不安全 tty 字符被拒绝；安装后制造文件为 `root:uhfgateway 0640`，升级默认保留；按三个配置 tty 生成精确 `DeviceAllow` drop-in | systemd policy、install tree、release build、installer、packaging policy smoke；installer 测试覆盖占位模板拒绝、三个外部文件复制/权限、升级保持和设备 allowlist | 已进入构建、发布和启动配置；首次启动停在 HTTPS 激活页直至绑定成功；目标板实际 tty/RS485、身份和密钥未确认，因此未真实安装或启动 |
| 根工程 `UHF_ENABLE_IEC61850` | ON 时编译并运行完整 libIEC61850 服务；OFF 时不再引用栈私有头或对象，使用明确不可启动的服务适配层，正式运行若未传 `--no-iec61850` 会在其他服务启动前报错 | OFF 构建 `uhf-gatewayd` 成功；自检、三 PTY v3 E2E、禁用门禁 3/3；ON 的 Debug/Release/ASan+UBSan 均复跑正式 E2E 与 MMS 模型 2/2；CI host 增加 OFF 构建 | 关闭 IEC 仅用于可裁剪主机/产品构建，不代表 v3 正式交付可以省略需求规定的 IEC 61850 |

三端口采集器已由 `GatewayRuntime --v3` 启动；未激活时仅激活 Web 监听存在，串口、Modbus、IEC、FTP、健康/数据 API 和 WebSocket 均不启动或明确拒绝。设置页和 `/api/v1/config` 可保存电流/温度串口 profile（重启生效），并热加载局放地址、三路采集间隔、超时/重试/迟到隔离、工程换算、三路时效及 12 个阈值。设置页根据正式快照 schema 只显示当前运行模式的采集与存储选项，隐藏项仍按原值原子回写，不会清空兼容配置。局放默认 3 秒的含义已按原文修正为“同一完整通道相邻两次读取的起始间隔”，不再错误地在 31 个分段请求之间逐笔等待。Web 快照返回实际生效参数，空 profile/换算/阈值不会伪造正常值。

新状态目录的总览默认标题为“综合监测管理机 / 三合一监测设备”；已有状态目录仍读取并保留用户已保存的标题，不执行覆盖迁移。

当前时效默认值仍是可替换的保守策略：局放 10 分钟、电流 5 秒、温度 5 秒。`v3_*_freshness_ms` 通过版本化配置校验、原子保存并热生效，允许范围暂定为 1 秒至 24 小时；Web 源状态回显实际生效期限。判定只使用单调时钟，展示/存档使用对应源 UTC 成功时间；Web 过期值为 `null`，Modbus 返回异常 04，IEC 保留最后有限值但标 `questionable/oldData`。默认期限、范围和三种上行表示尚未由产品确认。

## 3. 已测试但仍需产品确认的选择

- 电流编码和电流/温度换算倍率必须由配置或主机模拟命令行明确提供；不把未知单位猜成 A、mA 或 0.1℃。配置要求编码/倍率/偏移完整成组且倍率为正有限数，仓库默认使用 `unconfigured`/`null`；未提供时源状态可在线但工程值保持无效。当前转换辅助函数按一组通道同一线性倍率处理；若 CT/传感器倍率逐通道不同，需先补逐通道转换配置。
- 主需求只明确局放和上行 485-4 为 115200/8N1，没有给电流/温度串口参数。两路 profile 默认 `unconfigured`，此时正式进程使用不可用适配器而不是打开 `/dev/ttyS2`/`ttyS3` 猜 115200；保存 profile 后要求重启。当前支持常见 termios 波特率 1200/2400/4800/9600/19200/38400/57600/115200/230400，以及 7/8 数据位、N/E/O 校验、1/2 停止位；其他非标准波特率需目标板适配层。
- 新局放均值/峰值使用 mV；谱图只保留有符号原值，不标成 dBm，也不继承 PD1000 的 `FFBA/FFBC` 特殊值含义。
- 启动不足 50 个有效样本时，调用方必须选择“先报样本不足”或“按已有样本计算”。当前窗口按每相最近 50 个有效采样推进；无效样本跳过，不清空历史。断线/重新配置是否重置，需要进一步确认。
- 除零、溢出、非有限值标成无效；负均值不平衡度的分母提供按原公式、取绝对值、拒绝非正值三种显式策略，没有偷偷选生产默认值。
- 当前 `In/IN/TN` 输出百分数数值，例如 25 表示 25%；`Qa/Qb/Qc` 保持原公式比值，例如 0.25。需要与上位机确认数值语义。
- 电流/温度上行使用 **0x03** 是依据历史方案提出的配置；主需求 §5.1 对该区功能码未像局放 0x04 那样明确写出，发布前需确认。
- 无效 float32 可明确选择规范化 quiet NaN 或 Modbus 异常 04；缺失局放原始字、未知遥信状态和过期测量目前返回异常 04，不伪造正常零值。Web 输出 `null`；IEC 对有最后值的过期数据输出 `questionable/oldData`。这是暂定策略，最终约定仍须与上位机确认。
- 遥信按主需求表格顺序生成 0..14 的线地址，表版本为 1；完整 Modbus 点表由 `point_table_csv()` 从正式常量生成，并通过鉴权 Web 接口/设置页导出。编号可由程序决定，不要求需求方手工编表；配置变化不能偷偷重排地址。最终外部点表版本仍需和上位机共同冻结。
- 每次请求读取线程安全不可变快照；局放、电流、温度各自保留成功/尝试 UTC 时间。当前局放三通道共用该采集源最近一次成功时间；若最终点表要求逐通道独立时间，需要扩展状态结构。

## 4. 待澄清清单（按对联调的影响排序）

1. **电流/温度完整下行定义**（§3.2/3.3、附录 C/D）：两台设备的波特率、校验/停止位；电流 signed/unsigned、A/mA/CT 比例及逐通道倍率；温度 152 究竟表示 152℃ 还是 15.2℃；量程和设备故障码。温度 1/3/5 号寄存器已经明确，不是缺项。
2. **谱点物理含义**（§3.1/8）：单位、倍率、有效范围、无信号/未刷新/传感器故障编码。特征值 mV 不能直接推出谱点也为 mV。
3. **无效和过期数据的最终对外约定**（§5/7）：代码当前把局放 10 分钟、电流/温度 5 秒作为可热配置的暂定默认值，并采用 Web `null`、Modbus 异常 04、IEC `questionable/oldData`，存档记录三源时间/质量。仍需确认生产期限、允许范围、上位机期望以及局放逐通道时间；某个分段失败当前不发布该通道的部分新轮次。
4. **计算边界**（§4）：除数为零怎么办；负温度均值如何定义 TN；百分比线上传 25 还是 0.25；不足 50 次、读取失败、断线恢复、重启/修改周期时如何处理统计窗口。
5. **通信和报警时序**（§3/5.1/7）：局放完整通道间隔、电流/测温周期、超时、重试延时和迟到隔离均已显式配置；当前把“重试 3 次”解释为首次之外额外 3 次，局放 3 秒按相邻两次完整通道读取的开始时刻计算。仍需确认重试总次数语义、连续失败告警/恢复次数、断线退避上限、12 个默认阈值及无效数据时报警保留/复归。回差/防抖无依据时不擅自增加。
6. **IEC 61850 交付点表**（§5.2）：当前代码提供候选 SPDC1..3、MMXU1、STMP1、GGIO1 数据对象、q/t、两个报告数据集；主机测试已用真实 MMS 客户端读取 41 项测量和 15 项状态数据集、启用未缓冲测量 RCB 与缓冲状态 RCB，并收到统一快照产生的 Ia 数据变化报告及三次电流失败产生的通信告警报告，随后验证成功样本清除服务值。仍需产品列定最终 DO/DA、单位、报告控制块和目标客户端验收用例；“MMXU/STMP 类”和当前候选报告名还不是已冻结的互操作接口。相关性、谱图、全部计算值哪些需要上送也应列明。
7. **激活规范和制造配置**（§9）：确定序列号的真实来源、标识拼接格式/主路径、厂家密钥注入与轮换方式；只保存一个“已激活”布尔值不能替代启动时重新核对设备绑定。文中 20 字符、每组 5 个与 `XXXX-XXXX-XXXX-XXXX` 格式矛盾，Base32 字母表、去填充和金样本需固定。普通固件密钥可被提取的风险原文已经承认，不能把它验收为绝对防复制。
8. **目标板与配置边界**（§2/5.4）：确定最终 CPU/发行版、交叉工具链、四个 tty 与网卡名字、RS485 方向控制方式、Wi-Fi/4G 模块及其管理接口。现有构建基准是 AArch64；不能据“ARM/MIPS”推断两个平台都已验证。旧 PD1000/dBm 事件能力已从 v3 页面和 API 隔离；若 v3 仍需触发事件包，必须另行提供基于新单位、质量和触发条件的规则。另需确认 v3 是否保留 FTP 等旧服务。
9. **验收输入和阈值**：补三台设备的脱敏正常/故障报文、实测值对照、上位机读写/报告期望、允许刷新延迟/磁盘内存上限、连续运行时长。主机模拟测试不能替代这些实机验收。

以上多数问题不阻止框架和纯逻辑开发，但不能靠 AI 猜定之后直接声明整机完成。类名、文件拆分、点表生成器、缓存具体容器等普通设计细节，可继续交给实现者决定。

## 5. 本地验证

在仓库根目录：

```sh
cmake -S src/v3 -B build/v3 -DCMAKE_BUILD_TYPE=Debug
cmake --build build/v3 --parallel 4
ctest --test-dir build/v3 --output-on-failure

cmake -S src/v3 -B build/v3-release -DCMAKE_BUILD_TYPE=Release
cmake --build build/v3-release --parallel 4
ctest --test-dir build/v3-release --output-on-failure

cmake -S src/v3 -B build/v3-sanitize -DCMAKE_BUILD_TYPE=Debug -DUHF_V3_SANITIZERS=ON
cmake --build build/v3-sanitize --parallel 4
ctest --test-dir build/v3-sanitize --output-on-failure

cmake -S . -B build/host -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Debug -DUHF_ENABLE_IEC61850=ON
cmake --build build/host --parallel 4
ctest --test-dir build/host --output-on-failure
```

本轮上述三个独立 v3 构建各有 5 个 CTest 测试，Debug、Release、ASan+UBSan 均 5/5 通过；加入交互模拟器回归后的根工程为 72 项，最终 Debug 和 Release 均 72/72。Release 测试不依赖会被 `NDEBUG` 关闭的 `assert`。根工程包含真实守护进程 PTY 端到端、Web、Modbus、IEC MMS、激活、内存报文缓存和持久化测试；这些仍是主机模拟，不是目标板或真实上位机稳定性验收。

v3 参数配置增量另外在根工程 Release 下执行上行、配置、Web、正式守护进程四项回归（4/4），并在 ASan+UBSan 根工程构建下执行上行、配置及正式守护进程 PTY 端到端（3/3）。Release 首次运行暴露测试依赖自然经过 1ms 的非确定时序，改为显式旧单调时间后 Debug/Release/ASan+UBSan 均重跑通过。

采集时序增量的 Release 首次端到端运行在等待第二轮局放后，256 条报文缓存已按设计淘汰早期电流/温度条目，使旧的“三路同时仍在缓存”断言失败；测试已调整为先验证三路有界缓存，再独立等待并验证局放轮次间隔。调整后根工程 Debug 全量 71/71、Release 定向 4/4、ASan+UBSan 定向 3/3 通过。

三合一 Web 增量在当时源码上执行根工程 Debug 全量 71/71；Release 构建正式 `uhf-gatewayd` 后 Web/旧 WebSocket/v3 PTY E2E 4/4；ASan+UBSan 构建 `uhf-gatewayd` 和测试所需 `uhf-privilegedd` 后同组 4/4。当时一次 `cmake --build build/host-release -j2` 全目标命令在既有 `tests/network_config_smoke.cpp` 上失败：Release 的 `NDEBUG` 去掉 `assert` 后局部变量触发 `-Werror=unused-variable`；正式程序目标单独构建通过，且该测试设施问题已在后续增量修复。ASan 首次定向运行也因只构建了网关、缺少 `uhf-privilegedd` 可执行文件而有 1 项测试设施失败，补建明确依赖后原测试通过。

文件式激活输入增量在 Debug、Release、ASan+UBSan 三套根工程构建中均实际构建 `uhf-gatewayd` 和 `uhf_activation_smoke`，并分别运行激活测试与正式三 PTY v3 E2E，三套均 2/2 通过。

v3 安装启动增量在 host Debug、Release、ASan+UBSan 分别执行 `uhf_systemd_policy_smoke|uhf_install_tree_smoke|uhf_release_build_smoke|uhf_installer_smoke|uhf_packaging_policy_smoke`，三套最终均 5/5 通过；这是临时根目录内的发布/升级模拟，没有执行真实 systemd、写真实 `/etc` 或部署目标板。Release 首轮只构建 `uhf-gatewayd`，`uhf_install_tree_smoke` 因安装清单中的 `uhf-network-recovery` 尚未在该构建目录生成而失败；补建全部五个安装目标后原项重跑通过，属于测试前置产物不完整。

交互式主机模拟增量新增 `uhf_v3_host_sim_smoke`，对 Debug、Release、ASan+UBSan 三套正式守护进程各运行一次，最终均为 1/1 通过；测试要求三个 PTY 的样本和显式工程换算进入 schema 3 HTTP 快照，并检查模拟器可通过 SIGTERM 清理。手工首跑发现固定默认 Web 端口已被本机容器占用，入口改为默认选择空闲 localhost 端口并拒绝显式冲突；随后 ASan 并行复跑暴露“HTTP 已可读但就绪文字尚未输出”的测试竞态，测试改为从管道显式等待就绪标志，没有用固定延时掩盖。最终 Debug 根工程全量为 72/72。

v3/兼容页面隔离增量构建正式 `uhf-gatewayd` 与 Web 测试依赖，并在 Debug、Release、ASan+UBSan 分别执行 `uhf_web_preview_smoke|uhf_web_server_smoke|uhf_v3_gateway_e2e`，三套最终均 3/3。Debug 首轮新增兼容事件索引断言期望 200，但该测试刻意未配置数据目录，既有行为正确返回 503；断言改为验证 503，从而同时证明非 v3 运行时没有被误判为 v3，未改变产品路径。

Release 测试完整性增量把根工程剩余 9 个使用 C `assert` 的 C++ smoke 改成 `UHF_TEST_CHECK`，因此检查表达式在 `NDEBUG` 下仍执行；`cmake --build build/host-release -j2` 全目标成功，随后 Release 全量 72/72。首次并行运行 Debug 与 ASan 的 9 项回归时，两个 DHCP 测试进程争用同一个固定临时目录，Debug 为 8/9、ASan 为 9/9；相关网络测试目录加入 PID 后，Debug、Release、ASan+UBSan 三套并行复跑均 9/9。该增量没有修改产品源文件。

IEC 报告验证增量在 Debug、Release、ASan+UBSan 三套根工程构建中分别构建 `uhf_iec61850_server_smoke`，并执行 `ctest --test-dir BUILD --output-on-failure -R '^uhf_iec61850_v3_model_smoke$'`。测量 URCB 阶段三套均 1/1，Debug 与 Release 全量均为 72/72；增加状态 BRCB 后三套再次均为 1/1，且 Debug 兼容/v3 服务端回归为 2/2。测试经网络连接实际 MMS 服务，读取 `DSV3Measurements` 的 41 项映射并启用 `RPV3Measurements` 未缓冲 RCB，要求统一快照中的 Ia 从 1.0 更新为 9.0 时报告携带该成员的 `data-change` 原因和值；又读取 15 项 `DSV3State`、启用缓冲 `RPV3State`，连续三次电流失败后要求状态报告携带通信告警，成功样本后服务值和质量恢复；最后验证过期时仍保留 9.0 且质量为 `questionable/oldData`。这证明候选模型的主机端服务/客户端报告链路，不等于最终点表冻结或目标上位机互操作验收。

正式 IEC 主程序接入增量不再让 `uhf_v3_gateway_e2e` 对主运行实例传 `--no-iec61850`：测试为守护进程分配独立 localhost MMS 端口，MMS 客户端读取 41 项数据集并启用测量 URCB，在客户端明确就绪后把 PTY 电流设备的 Ia 从 10 改为 30，要求收到对应 `data-change` 报告，并从同一正式守护进程读回值 30 和 `good` 质量。Debug、Release、ASan+UBSan 定向测试均为 1/1，最终源码 Debug 全量为 72/72；IEC 被构建关闭时测试仍保持原有非 IEC 路径。该测试把三路 PTY、采集调度、统一快照和实际 MMS 服务接成同一进程，但仍不是真实下位机或目标上位机互操作验收。

正式 Modbus RTU 主程序接入增量将第四个 PTY 作为上行 485 传给同一个 `uhf-gatewayd --v3`，不再对主实例传 `--no-modbus-rtu`。IEC 探针把下行电流样本切到 Ia=30 后，测试通过 RTU 功能码 0x03 从文字地址 1 读取两个寄存器，独立解码出高字在前 float32 30.0；再用 0x02 从线地址 6 读取已热加载的 Ia 高报警，并对两个响应独立核对 CRC。Debug、Release、ASan+UBSan 定向测试均为 1/1；协议边界、非法请求和错误 CRC 仍由 `v3_register_map`/`uhf_modbus_rtu_smoke` 覆盖。

正式 Modbus/TCP 主程序接入测试使用独立事务号读取同一 Ia 文字地址 1 的两个寄存器，核对 MBAP 事务号、协议号、长度、单元号、功能码和字节数，并按高字在前 float32 独立解码为与同轮 IEC/RTU 一致的 30.0（无 IEC 构建路径为 10.0）。这证明 TCP 服务消费正式统一快照，而不再只判断响应形状；畸形 MBAP、地址边界和异常码仍由协议测试覆盖。正式 E2E 在 Debug、Release、ASan+UBSan 各为 1/1，无 IEC helper 的兼容执行也通过。

正式历史接入增量在同一 E2E 中等待守护进程首次非零 generation 自动保存，要求 `data/v3` 中确有二进制记录、鉴权后的 `/api/v1/frames` 返回 schema 3 非空索引，且 `/api/v1/frames/export.csv` 返回 35 项数据行；初始倍率未配置时 Ia 必须记录为 `valid=0` 且空值，不能伪装为正常零。首轮测试发现下载响应实际缺少文件名闭合双引号（`filename="latest-frame.csv`），已修复共享 Web 导出头并保留严格断言。最终 Web/兼容历史/正式 v3 E2E 在 Debug、Release、ASan+UBSan 均为 3/3。

提交 `f902808` 的 Actions 中三个独立 v3 core job 均成功，host 回归的 72 项中 71 项成功；失败的是并行运行的既有 `uhf_runtime_modbus_rtu_smoke`，日志为等待 RTU 响应 2 秒超时，新 `uhf_v3_gateway_e2e` 已成功。审查发现旧 smoke 创建 PTY 后未在启动网关前切 raw，负载高时二进制请求可能先经过默认终端回显/规范行规程；测试现预先将 PTY 两端设为 raw，未放宽原响应超时。本地按 CI 的并行度将旧 RTU 与 v3 E2E 各重复 10 次为 20/20，并在 Release、ASan+UBSan 各并行复跑 2/2；最终源码 Debug 按 CI 并行度全量为 72/72。修复提交的 Actions 结果仍以对应远端运行记录为准。

后续修复提交 `5b351f5` 的 Actions 运行 `35443323814` 已全部成功：三个 v3 core Debug/Release/Sanitizers 作业和包含上述旧 RTU smoke 的 72 项 host 回归均为绿。

提交 `1415a26` 的 Actions 运行 `35443564204` 同样全部成功。激活门禁提交 `889b824` 的运行 `35443919703` 中三个 v3 core 作业成功，但 host Test 达到 20 分钟上限而取消；上传的 CTest checkpoint 显示正式 v3 E2E 已成功，既有 `uhf_dhcp_client_smoke` 是唯一未完成项，`uhf_persistence_runtime_smoke` 另在 `event frame bound` 失败。DHCP fixture 原用固定 50ms 猜测子进程已完成 `exec`，慢调度下会在 exec 前拒绝结束子进程，随后无界 `waitpid`；现改用 `O_CLOEXEC` 管道确认 exec 边界并以 2 秒有界回收防止 CI 再次挂死。持久化 fixture 原按固定 150ms 发布 5 帧，线程受压时可能跳过中间 generation；现将该测试的周期设为显式逐帧保存，并在发布下一帧前等待保存计数推进，仍严格要求事件包包含 5 帧且保存计数恰为 5。

两项修复在 Debug 并行首次为 2/2，随后各重复 10 次均通过；Release、ASan+UBSan 各为 2/2，最终 Debug 按 CI 并行度全量为 72/72。修复未放宽事件帧数、子进程匹配或退出结果断言。

根工程的 `UHF_ENABLE_IEC61850=OFF` 原先仍无条件编译 `model.cpp/server.cpp` 并从公共头包含栈头文件，导致选项关闭后 `uhf-gatewayd` 无法构建。现由 CMake 给所有目标提供明确能力宏，网关仅在 ON 时加入真实模型/服务及栈依赖，OFF 时加入 `server_disabled.cpp`；`GatewayRuntime` 对误启用 IEC 在采集和网络服务启动前返回明确错误。独立 OFF 构建的自检、无 IEC 正式 v3 E2E 和禁用门禁为 3/3；真实 IEC ON 的 Debug、Release、ASan+UBSan 中正式 E2E 与 MMS 模型均为 2/2。提交 `710d3f5` 的 Actions 运行 `35445962150` 四个作业全部成功，host 作业实际执行了新增的 OFF 配置、构建和三项测试。

正式激活门禁增量从 `uhf-gatewayd --v3` 分别传入错误激活码，以及复用已激活状态但更换设备标识；两次都必须以激活错误退出。测试在进程退出后要求三个下行 PTY 请求计数完全不变、激活失败不生成状态文件，确认 Web、Modbus/TCP 及启用时的 IEC 端口均未监听，并向独立上行 PTY 写入合法 RTU 请求后确认无响应，证明限制位于正式后端服务创建之前，而不是网页隐藏。正式 E2E 在 Debug、Release、ASan+UBSan 各为 1/1，无 IEC helper 路径也通过。

继续审计发现旧的 `--no-acquisition` 分支不会创建 `GatewayRuntime`，修复前 `--web --v3 --no-acquisition` 可在没有身份、密钥和激活状态时持续运行 Web。现在 `src/main.cpp` 对所有 v3 启动先执行进程级绑定复核，成功首次激活后清除一次性请求码，再由 `GatewayRuntime` 从持久化状态独立复核；Web-only 兼容入口不再绕过门禁。正式 E2E 新增未激活 Web-only 启动，要求返回激活错误、端口未监听且不生成 `activation.state`。该用例在 IEC ON 的 Debug、Release、ASan+UBSan 及 IEC OFF 构建均为 1/1，最终 Debug 全量为 72/72；提交 `007e5d6` 的 Actions 运行 `35446358416` 四个作业全部成功。

同一轮审计确认原 `PosixSerialPort` 只在构造时打开一次：配置过的某一路 tty 启动缺失会使整个 `GatewayRuntime` 构造失败，运行中 `POLLHUP/EIO` 也不会重开。v3 三路下行现改用 `ReconnectingSerialPort`；它保留设备路径和 profile，打开失败仅向本源采集返回失败，致命 I/O 后丢弃旧描述符，并在下一次重试/轮询重新打开。`uhf_serial_port_smoke` 使用真实 PTY 与稳定符号链接验证“启动缺失 → 出现可用 → 断开 → 替换 PTY → 双向恢复”；正式 E2E 先将电流稳定路径保持缺失，要求记录失败的同时局放在线，再运行中链接真实 PTY并要求电流恢复、未知换算保持无效；提交 `884ccec` 的 Actions 运行 `35448447983` 四个作业全部成功。随后正式主实例也改由稳定电流路径驱动：已在线设备断开至三次完整轮询失败后，Web 通信告警和整体采集健康转 `down`，Ia 无效，局放/温度仍在线，Modbus/TCP 电流通信故障遥信为 1；替换 PTY 后要求失败计数和告警清零、健康 `up`、遥信为 0 且 Ia=42。扩展用例当前在 Debug、Release、ASan+UBSan 和 IEC OFF 各为 1/1。这些是主机 PTY 证据，目标板 tty/RS485 热插拔仍未验证。

上行 Modbus/RTU 随后也改用同一可重连适配层，避免默认 `/dev/ttyS4` 缺失时拖垮其余正式服务；RTU 服务在打开/读取失败后等待 100 ms，防止缺失设备造成忙循环，并在断线时清空旧端口残帧。正式 E2E 现在让守护进程在 RTU 稳定路径不存在时先完成三路采集、Web、TCP 和 IEC 验证，随后创建指向第四个 PTY 的链接，并轮询正式健康接口确认进程已经打开替换设备后再发送 0x03/0x02 请求。首次直接在建链后发送的 Debug E2E 因 tty 首次打开时清空了提前到达的字节而 2/3，该测试竞态改用上述明确打开条件。连接态第一次编译又因 `connected()` 声明误落在基础串口类而失败，移动到可重连类后完成验证。最终健康接口在实际打开前为 `down`、打开后为 `up`，Debug、Release、ASan+UBSan 定向均为 4/4，Debug 全量 72/72。IEC OFF 的一次扩展选择为 3/4，失败的旧 `uhf_runtime_modbus_rtu_smoke` 默认仍启用已裁掉的 IEC；CMake 现只在 OFF 时向它显式传 `--no-iec61850`，并将该项加入 OFF CI，最终扩展集合为 6/6、CI 规定集合为 4/4。提交 `8704b6b` 的 Actions 运行 `35447864704` 四个作业全部成功。之后的正式 E2E 进一步在已成功轮询的旧 PTY 上只发送半帧后断开，要求健康转回 `down`；替换 PTY 出现并重新为 `up` 后，完整请求必须正常返回，证明旧流残帧没有跨设备混入。该用例在 Debug、Release、ASan+UBSan 和 IEC OFF 构建各为 1/1；提交 `ee5cd10` 的 Actions 运行 `35448149457` 四个作业全部成功。

`.github/workflows/v3-host-validation.yml` 分别运行旧产品 host 回归和新核心 Debug/Release/ASan+UBSan 测试。是否通过以对应提交的 Actions 结果为准；不要把某个旧提交的绿灯当成当前提交已通过。

## 6. 后续最小闭环

实时总览已经消费正式 schema 3：可切换三个局放通道，展示七项特征、设备谱点原始值、三源状态/独立时间/运行参数、35 项工程值及质量、12 点报警；WebSocket v3 推送也由正式 PTY 守护进程端到端验证。页面不再含 PD1000、单通道或 dBm 假设。正式发布前还必须完成目标盘压力/真实耗尽测试、最终 IEC 点表/报告互操作、制造激活参数注入、板级串口/RS485 方向控制适配和整机验收。

激活交互增量按原文 §9.6 增加 `activation.html` 和 `/api/v1/activation`：未绑定时 GET 只返回设备标识，POST 要求同源 JSON，错误码不生成状态；成功后原子写入 `0600` 状态并重新复核绑定，关闭专用监听，再在同一进程创建三路采集和全部上行服务。激活专用模式对快照、健康、登录、配置、WebSocket 和其他静态页面均拒绝或重定向；正式 E2E 同时断言 Modbus/TCP、RTU、IEC 和三个 PTY 在激活前没有活动。随包 systemd/安装器只注入 tty、设备标识和厂家密钥，不再把激活码保存为 `/etc` 文件或传入命令行；显式 `--v3-activation-code-file` 仅保留为开发/制造自动化兼容入口。失败频控暂定为同一来源 60 秒窗口内第 5 次失败后阻断 30 秒，修改入口是 `http_server.cpp` 的 `kLoginFailureWindow/kLoginBlockTime`，尚未标记为用户确认。该增量本地 Debug 全量 72/72；Debug、Release、ASan+UBSan、IEC OFF 的 Web 资源与正式 E2E 各 2/2；systemd policy、installer 和 Web 资源定向测试各通过。真实制造参数和目标板激活流程仍未验证，不能称为生产激活验收通过。
