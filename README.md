# UHF 61850 局放监测管理机

本仓库用于 Nuvoton MA35D1-IoT（ARM64 / Ubuntu 22.04）局放监测管理机的研发。

当前阶段先在开发机完成产品代码和协议模拟器。实现应先以模拟器完成协议和异常场景测试，再部署到实机验证串口、双网口和 IEC 61850 互操作。

## 设计资料

- [需求基线](docs/requirements-baseline.md)：已确认的范围、验收编号、需求冲突与待确认项。
- [总体架构](docs/architecture.md)：运行时、模块边界、数据一致性、Web/登录、配置、日志和部署设计。
- [研发机评估](docs/machine-audit.md)：目标板资源、当前服务、可复用内容与限制。
- [实施与验收计划](docs/implementation-plan.md)：供后续 Luna Max 执行的工作包、测试矩阵和交付门槛。
- [Luna Max 小步实现简报](docs/luna-implementation-brief.md)：已确认决策、通用 IEC 61850 映射和逐提交开发顺序。
- [本地构建](docs/build.md)：host/AArch64 构建、CTest 和发布材料说明。

## 当前结论

- 目标板足以运行本项目，也适合作为串口/网络/IEC 61850 集成测试机。
- 目标板不适合作为主要编译开发机；应在工作站或 CI 交叉编译 ARM64 发布包，再部署到板卡。
- `/data` 中旧 Python 网关只作为硬件映射和页面风格参考，不作为新项目代码基线。
- `frpc.service` 与 `4g_server` 不属于本项目，任何安装、测试和网络配置都不得停止、覆盖或接管它们。
- 产品允许按 GPLv3 兼容方式开源，使用 libIEC61850 1.6.2，无需购买商业许可证。
- IEC 61850 采用标准 `SPDC` 局放节点加 `GGIO` 五值映射；第一版只做浏览、读值和一个 URCB。
- 内置被动 FTP 可在可信管理网完成专用目录文件上传/下载，使用当前 Web 管理员账号认证；MMS 文件服务仍保持关闭。
- Luna Max 按小功能增量实现，每次测试通过后提交并推送，避免大批量改动。

## 本地构建

首个构建骨架可用以下命令验证：

    bash tools/build-local.sh

## FTP 文件传输

在“系统维护 → 采集与转发”启用 FTP 并重启服务后，使用被动模式连接设备 IP（默认端口 21），账号为 `admin`，密码与 Web 当前管理员密码相同。服务只能看到 `/var/lib/uhf-gateway/ftp`，不允许访问配置、日志或采集数据目录。传统 FTP 为明文协议，仅应在隔离或可信管理网使用。

## 源材料

- `嵌入式开发需求书-局放监测管理机-v1.0.docx`
- `寄存器对应表（modbus-rtu）PD1000.20260825183514491.xlsx`
- 目标板 `/data` 旧实现（只读参考）

仓库中不得保存目标机 SSH 私钥、Git 密码、初始管理员明文密码或其他运行时密钥。
